{{>header}}
{{>navbar}}
<section class="container my-4">
    <h3 class="text-center text-primary my-3">ِAdd Transaction</h3>
    <form method="post"> 
     
        <div class="mb-3">
            <input type="number" name="value" class="form-control" placeholder="Enter Your Value">
        </div>
   
   
        <div class="mb-3">
            <select name="transType" class="form-control">
                <option selected disabled>please choose your Transaction type</option>
                {{#each transType}}
                <option value="{{this}}">{{this}}</option>
                {{/each}}
            </select>
        </div>
        <div class="mb-3">
            <button class="btn btn-primary">Add Transaction</button>
        </div>

    </form>
</section>
{{>footer}}